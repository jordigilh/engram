# Pinned evaluation reproduction status (historical)

Checked 2026-09-23 against the pinned Kubernaut feature snapshot recorded in
`kubernaut_fix-2442_8f3bc5a2_2026-09-23.json`.

## Verified

- The staged source root is `/var/folders/r7/gktmmltd1zq7wqhsjjslwsm80000gn/T/opencode/kubernaut-fix-2442-8f3bc-zvec`.
- Its filtered inventory is 1,072 Go files and 13,611,586 bytes, matching the
  recorded scope and byte count. The benchmark records a selected-source
  digest of `1eb2a57192e0af48c80741d7d732a13028f93c2614d194e3ce278234cc859dd8`,
  recomputed with the recovered algorithm: SHA-256 over sorted relative path,
  NUL, file bytes, NUL for each selected file. The staged root and both clean
  copies produce the recorded digest.
- Sense v1.15.1 was rebuilt locally against that root with embeddings enabled.
  It scanned 1,072 files, indexed 1,053 Go files, and produced full ranked
  JSON for all 11 active base and follow-up queries. The raw run is preserved
  in `kubernaut_fix-2442_8f3bc5a2_2026-09-23.sense-1.15.1-full.json`.
- A disposable CocoIndex table was built from the same verified files with
  19,371 rows across 1,072 files and full hybrid top-10 output is preserved in
  `kubernaut_fix-2442_8f3bc5a2_2026-09-23.cocoindex-isolated-full.json`.
- A separate zvec index was rebuilt from the same verified files with 17,629
  entities and full top-10 MCP snippets are preserved in
  `kubernaut_fix-2442_8f3bc5a2_2026-09-23.zvec-rebuilt-full.json`.

## Blocked

- The preserved zvec index cannot be opened by either the current release or
  debug `zg` binary. Direct and daemon requests fail while opening the pinned
  `embedding.index.3.proxima` collection with `ZG.ENGINE.STORAGE_FAILURE`.
  No zvec results were substituted from the live daemon or shadow logs; the
  rebuilt zvec run is explicitly separate from the preserved index.

## Evaluation decision at the time of recovery

The three-backend full-result prerequisite is now satisfied, but do not score
or publish backend quality yet. Normalize the three runs, blind backend
identity, add source-found answers, and adjudicate every pooled candidate
against the verified source before using the fail-closed evaluator.

## Next recovery action

Keep the disposable CocoIndex table and rebuilt zvec root until normalized
runs and qrels are frozen. Do not use the live Kubernaut checkout, current
shared table, or shadow JSONL logs.

## Subsequent result

The three saved runs were normalized against the verified source, the pooled
300 candidates were reviewed, and the adjudicated qrels and `metrics-k10.json`
were preserved in this directory. The broken *original* zvec index was not
recovered; its replacement is explicitly `zvec-rebuilt-full.json`. This is
exploratory real-repository evidence, separate from the source-authored
[synthetic qeval protocol](./SYNTHETIC_EVALUATION.md) used for retrieval
regression decisions.
