{
  "schema_version": 1,
  "suite_id": "synthetic-python-workflow-discovery",
  "fixture_id": "python-workflow-discovery-v1",
  "source": {
    "repository": "synthetic/python-workflow-discovery",
    "revision": "fixture-v1",
    "root": ".",
    "include": [
      "**/*.py"
    ],
    "exclude": [
      "**/test_*.py"
    ],
    "snapshot_sha256": "0b77b06131e0a0e8ffbafa258c54e8900b7c32c2ea6f156ea75e8c6a181e076b",
    "files": 8,
    "bytes": 6179
  },
  "result_limit": 10,
  "backend": "typescript-baseline",
  "binary_sha256": "817ff1c4ff0b57b0c27eb9c3d8869f9a4db32d1765267798e77c3be6d1b49f6f",
  "workspace_root": "/private/var/folders/r7/gktmmltd1zq7wqhsjjslwsm80000gn/T/opencode/zvec-four-language-final-v4.EG5Zqi/qeval-typescript-baseline-python-EG5Zqi",
  "queries": [
    {
      "id": "record-discovered-membership",
      "query": "Where are workflow IDs returned by list_workflows recorded for later validation?",
      "response": {
        "stdout": "query groups (1):\nQ1 [primary]: Where are workflow IDs returned by list_workflows recorded for later validation?\nhits: 10\n\n#1 matchedBy=fts+vector discovery/state.py:6-16\nsource:\n6\t@dataclass\n7\tclass WorkflowState:\n8\t    \"\"\"Hold workflow IDs returned by discovery for later validation.\"\"\"\n9\t\n10\t    ids: set[str] = field(default_factory=set)\n11\t\n12\t    def add(self, workflow_ids: list[str]) -> None:\n13\t        self.ids.update(workflow_id for workflow_id in workflow_ids if workflow_id)\n14\t\n15\t    def contains(self, workflow_id: str) -> bool:\n16\t        return workflow_id in self.ids\n\n#2 matchedBy=fts+vector tools/workflows.py:21-24\nsource:\n21\tdef list_workflows(catalog: WorkflowCatalog, state: WorkflowState) -> list[Workflow]:\n22\t    workflows = catalog.list_workflows()\n23\t    record_discovered_workflows(state, [workflow.workflow_id for workflow in workflows])\n24\t    return workflows\n\n#3 matchedBy=fts+vector discovery/state.py:12-13\nsource:\n12\tdef add(self, workflow_ids: list[str]) -> None:\n13\t        self.ids.update(workflow_id for workflow_id in workflow_ids if workflow_id)\n\n#4 matchedBy=fts+vector discovery/catalog.py:14-15\nsource:\n14\tdef __init__(self, workflows: list[Workflow]) -> None:\n15\t        self.workflows = {workflow.workflow_id: workflow for workflow in workflows}\n\n#5 matchedBy=fts+vector discovery/catalog.py:13-27\nsource:\n13\tclass WorkflowCatalog:\n14\t    def __init__(self, workflows: list[Workflow]) -> None:\n15\t        self.workflows = {workflow.workflow_id: workflow for workflow in workflows}\n16\t\n17\t    def list_workflows(self, filters: dict[str, str] | None = None) -> list[Workflow]:\n18\t        workflows = list(self.workflows.values())\n19\t        if not filters:\n20\t            return workflows\n21\t        return [workflow for workflow in workflows if all(workflow.labels.get(k) == v for k, v in filters.items())]\n22\t\n23\t    def list_actions(self, filters: dict[str, str]) -> list[Workflow]:\n24\t        return self.list_workflows(filters)\n25\t\n26\t    def get_workflow(self, workflow_id: str) -> Workflow | None:\n27\t        return self.workflows.get(workflow_id)\n\n#6 matchedBy=fts+vector discovery/catalog.py:17-21\nsource:\n17\tdef list_workflows(self, filters: dict[str, str] | None = None) -> list[Workflow]:\n18\t        workflows = list(self.workflows.values())\n19\t        if not filters:\n20\t            return workflows\n21\t        return [workflow for workflow in workflows if all(workflow.labels.get(k) == v for k, v in filters.items())]\n\n#7 matchedBy=fts+vector tools/workflows.py:27-28\nsource:\n27\tdef record_discovered_workflows(state: WorkflowState, workflow_ids: list[str]) -> None:\n28\t    state.add(workflow_ids)\n\n#8 matchedBy=fts+vector tools/workflows.py:35-37\nsource:\n35\tdef authorize_selection_driver(workflow_id: str, result: DiscoveryResult) -> None:\n36\t    if not is_workflow_in_discovery_result(workflow_id, result):\n37\t        raise ValueError(\"workflow was not returned by discovery\")\n\n#9 matchedBy=fts+vector discovery/filters.py:26-36\nsource:\n26\tdef apply_label_filters(workflows: list[object], filters: DiscoveryFilters) -> list[object]:\n27\t    expected = {\n28\t        key: value\n29\t        for key, value in {\n30\t            \"component\": filters.component,\n31\t            \"environment\": filters.environment,\n32\t            \"severity\": filters.severity,\n33\t        }.items()\n34\t        if value\n35\t    }\n36\t    return [workflow for workflow in workflows if all(getattr(workflow, \"labels\", {}).get(k) == v for k, v in expected.items())]\n\n#10 matchedBy=fts+vector selection/retry.py:6-11\nsource:\n6\tdef select_with_retry(validator: Validator, workflow_id: str, corrected_id: str) -> str:\n7\t    try:\n8\t        validator.validate(workflow_id)\n9\t        return workflow_id\n10\t    except Exception:\n11\t        return self_correct_selection(validator, corrected_id)\n",
        "stderr": ""
      }
    },
    {
      "id": "reject-undiscovered-workflow",
      "query": "How is a catalog-valid workflow rejected when it was not returned by workflow discovery?",
      "response": {
        "stdout": "query groups (1):\nQ1 [primary]: How is a catalog-valid workflow rejected when it was not returned by workflow discovery?\nhits: 10\n\n#1 matchedBy=fts+vector selection/validator.py:10-25\nsource:\n10\tclass Validator:\n11\t    def __init__(self, allowed_ids: set[str]) -> None:\n12\t        self.allowed_ids = allowed_ids\n13\t        self.workflow_state: WorkflowState | None = None\n14\t\n15\t    def set_workflow_state(self, state: WorkflowState) -> None:\n16\t        self.workflow_state = state\n17\t\n18\t    def is_allowed(self, workflow_id: str) -> bool:\n19\t        if workflow_id not in self.allowed_ids:\n20\t            return False\n21\t        return self.workflow_state is None or self.workflow_state.contains(workflow_id)\n22\t\n23\t    def validate(self, workflow_id: str) -> None:\n24\t        if not self.is_allowed(workflow_id):\n25\t            raise ValidationError(\"workflow was not in the current discovery result\")\n\n#2 matchedBy=fts+vector tools/workflows.py:31-32\nsource:\n31\tdef is_workflow_in_discovery_result(workflow_id: str, result: DiscoveryResult) -> bool:\n32\t    return workflow_id == result.recommended or workflow_id in (result.alternatives or [])\n\n#3 matchedBy=fts+vector selection/validator.py:18-21\nsource:\n18\tdef is_allowed(self, workflow_id: str) -> bool:\n19\t        if workflow_id not in self.allowed_ids:\n20\t            return False\n21\t        return self.workflow_state is None or self.workflow_state.contains(workflow_id)\n\n#4 matchedBy=fts+vector discovery/catalog.py:17-21\nsource:\n17\tdef list_workflows(self, filters: dict[str, str] | None = None) -> list[Workflow]:\n18\t        workflows = list(self.workflows.values())\n19\t        if not filters:\n20\t            return workflows\n21\t        return [workflow for workflow in workflows if all(workflow.labels.get(k) == v for k, v in filters.items())]\n\n#5 matchedBy=fts+vector discovery/catalog.py:13-27\nsource:\n13\tclass WorkflowCatalog:\n14\t    def __init__(self, workflows: list[Workflow]) -> None:\n15\t        self.workflows = {workflow.workflow_id: workflow for workflow in workflows}\n16\t\n17\t    def list_workflows(self, filters: dict[str, str] | None = None) -> list[Workflow]:\n18\t        workflows = list(self.workflows.values())\n19\t        if not filters:\n20\t            return workflows\n21\t        return [workflow for workflow in workflows if all(workflow.labels.get(k) == v for k, v in filters.items())]\n22\t\n23\t    def list_actions(self, filters: dict[str, str]) -> list[Workflow]:\n24\t        return self.list_workflows(filters)\n25\t\n26\t    def get_workflow(self, workflow_id: str) -> Workflow | None:\n27\t        return self.workflows.get(workflow_id)\n\n#6 matchedBy=fts+vector tools/workflows.py:35-37\nsource:\n35\tdef authorize_selection_driver(workflow_id: str, result: DiscoveryResult) -> None:\n36\t    if not is_workflow_in_discovery_result(workflow_id, result):\n37\t        raise ValueError(\"workflow was not returned by discovery\")\n\n#7 matchedBy=fts+vector selection/validator.py:23-25\nsource:\n23\tdef validate(self, workflow_id: str) -> None:\n24\t        if not self.is_allowed(workflow_id):\n25\t            raise ValidationError(\"workflow was not in the current discovery result\")\n\n#8 matchedBy=fts+vector discovery/state.py:6-16\nsource:\n6\t@dataclass\n7\tclass WorkflowState:\n8\t    \"\"\"Hold workflow IDs returned by discovery for later validation.\"\"\"\n9\t\n10\t    ids: set[str] = field(default_factory=set)\n11\t\n12\t    def add(self, workflow_ids: list[str]) -> None:\n13\t        self.ids.update(workflow_id for workflow_id in workflow_ids if workflow_id)\n14\t\n15\t    def contains(self, workflow_id: str) -> bool:\n16\t        return workflow_id in self.ids\n\n#9 matchedBy=fts+vector tools/workflows.py:40-47\nsource:\n40\tdef handle_select_workflow(\n41\t    workflow_id: str, result: DiscoveryResult, catalog: WorkflowCatalog\n42\t) -> WorkflowResult:\n43\t    authorize_selection_driver(workflow_id, result)\n44\t    workflow = catalog.get_workflow(workflow_id)\n45\t    if workflow is None:\n46\t        raise KeyError(workflow_id)\n47\t    return apply_selected_workflow(workflow, result)\n\n#10 matchedBy=fts+vector tools/workflows.py:21-24\nsource:\n21\tdef list_workflows(catalog: WorkflowCatalog, state: WorkflowState) -> list[Workflow]:\n22\t    workflows = catalog.list_workflows()\n23\t    record_discovered_workflows(state, [workflow.workflow_id for workflow in workflows])\n24\t    return workflows\n",
        "stderr": ""
      }
    },
    {
      "id": "forward-labels-to-discovery",
      "query": "How are signal labels converted into filters before workflow discovery lists actions?",
      "response": {
        "stdout": "query groups (1):\nQ1 [primary]: How are signal labels converted into filters before workflow discovery lists actions?\nhits: 10\n\n#1 matchedBy=fts+vector discovery/catalog.py:23-24\nsource:\n23\tdef list_actions(self, filters: dict[str, str]) -> list[Workflow]:\n24\t        return self.list_workflows(filters)\n\n#2 matchedBy=fts+vector discovery/catalog.py:13-27\nsource:\n13\tclass WorkflowCatalog:\n14\t    def __init__(self, workflows: list[Workflow]) -> None:\n15\t        self.workflows = {workflow.workflow_id: workflow for workflow in workflows}\n16\t\n17\t    def list_workflows(self, filters: dict[str, str] | None = None) -> list[Workflow]:\n18\t        workflows = list(self.workflows.values())\n19\t        if not filters:\n20\t            return workflows\n21\t        return [workflow for workflow in workflows if all(workflow.labels.get(k) == v for k, v in filters.items())]\n22\t\n23\t    def list_actions(self, filters: dict[str, str]) -> list[Workflow]:\n24\t        return self.list_workflows(filters)\n25\t\n26\t    def get_workflow(self, workflow_id: str) -> Workflow | None:\n27\t        return self.workflows.get(workflow_id)\n\n#3 matchedBy=fts+vector discovery/catalog.py:17-21\nsource:\n17\tdef list_workflows(self, filters: dict[str, str] | None = None) -> list[Workflow]:\n18\t        workflows = list(self.workflows.values())\n19\t        if not filters:\n20\t            return workflows\n21\t        return [workflow for workflow in workflows if all(workflow.labels.get(k) == v for k, v in filters.items())]\n\n#4 matchedBy=fts+vector tools/workflows.py:31-32\nsource:\n31\tdef is_workflow_in_discovery_result(workflow_id: str, result: DiscoveryResult) -> bool:\n32\t    return workflow_id == result.recommended or workflow_id in (result.alternatives or [])\n\n#5 matchedBy=fts+vector discovery/filters.py:18-23\nsource:\n18\tdef filters_from_signal(signal: Signal) -> DiscoveryFilters:\n19\t    return DiscoveryFilters(\n20\t        component=signal.labels.get(\"component\", \"\"),\n21\t        environment=signal.labels.get(\"environment\", \"\"),\n22\t        severity=signal.labels.get(\"severity\", \"\"),\n23\t    )\n\n#6 matchedBy=fts+vector discovery/filters.py:6-8\nsource:\n6\t@dataclass\n7\tclass Signal:\n8\t    labels: dict[str, str]\n\n#7 matchedBy=fts+vector discovery/filters.py:26-36\nsource:\n26\tdef apply_label_filters(workflows: list[object], filters: DiscoveryFilters) -> list[object]:\n27\t    expected = {\n28\t        key: value\n29\t        for key, value in {\n30\t            \"component\": filters.component,\n31\t            \"environment\": filters.environment,\n32\t            \"severity\": filters.severity,\n33\t        }.items()\n34\t        if value\n35\t    }\n36\t    return [workflow for workflow in workflows if all(getattr(workflow, \"labels\", {}).get(k) == v for k, v in expected.items())]\n\n#8 matchedBy=fts+vector tools/workflows.py:35-37\nsource:\n35\tdef authorize_selection_driver(workflow_id: str, result: DiscoveryResult) -> None:\n36\t    if not is_workflow_in_discovery_result(workflow_id, result):\n37\t        raise ValueError(\"workflow was not returned by discovery\")\n\n#9 matchedBy=fts+vector selection/validator.py:15-16\nsource:\n15\tdef set_workflow_state(self, state: WorkflowState) -> None:\n16\t        self.workflow_state = state\n\n#10 matchedBy=fts+vector selection/validator.py:23-25\nsource:\n23\tdef validate(self, workflow_id: str) -> None:\n24\t        if not self.is_allowed(workflow_id):\n25\t            raise ValidationError(\"workflow was not in the current discovery result\")\n",
        "stderr": ""
      }
    },
    {
      "id": "preserve-membership-on-retry",
      "query": "How does discovered workflow membership survive self-correction retries?",
      "response": {
        "stdout": "query groups (1):\nQ1 [primary]: How does discovered workflow membership survive self-correction retries?\nhits: 10\n\n#1 matchedBy=fts+vector tools/workflows.py:50-51\nsource:\n50\tdef apply_selected_workflow(workflow: Workflow, result: DiscoveryResult) -> WorkflowResult:\n51\t    return WorkflowResult(workflow.workflow_id, lookup_discovered_parameters(workflow.workflow_id, result))\n\n#2 matchedBy=fts+vector tools/workflows.py:21-24\nsource:\n21\tdef list_workflows(catalog: WorkflowCatalog, state: WorkflowState) -> list[Workflow]:\n22\t    workflows = catalog.list_workflows()\n23\t    record_discovered_workflows(state, [workflow.workflow_id for workflow in workflows])\n24\t    return workflows\n\n#3 matchedBy=fts+vector discovery/catalog.py:14-15\nsource:\n14\tdef __init__(self, workflows: list[Workflow]) -> None:\n15\t        self.workflows = {workflow.workflow_id: workflow for workflow in workflows}\n\n#4 matchedBy=fts+vector tools/workflows.py:27-28\nsource:\n27\tdef record_discovered_workflows(state: WorkflowState, workflow_ids: list[str]) -> None:\n28\t    state.add(workflow_ids)\n\n#5 matchedBy=fts+vector tools/workflows.py:54-56\nsource:\n54\tdef lookup_discovered_parameters(workflow_id: str, result: DiscoveryResult) -> dict[str, str]:\n55\t    source = \"recommended\" if workflow_id == result.recommended else \"alternative\"\n56\t    return {\"source\": source}\n\n#6 matchedBy=fts+vector selection/validator.py:10-25\nsource:\n10\tclass Validator:\n11\t    def __init__(self, allowed_ids: set[str]) -> None:\n12\t        self.allowed_ids = allowed_ids\n13\t        self.workflow_state: WorkflowState | None = None\n14\t\n15\t    def set_workflow_state(self, state: WorkflowState) -> None:\n16\t        self.workflow_state = state\n17\t\n18\t    def is_allowed(self, workflow_id: str) -> bool:\n19\t        if workflow_id not in self.allowed_ids:\n20\t            return False\n21\t        return self.workflow_state is None or self.workflow_state.contains(workflow_id)\n22\t\n23\t    def validate(self, workflow_id: str) -> None:\n24\t        if not self.is_allowed(workflow_id):\n25\t            raise ValidationError(\"workflow was not in the current discovery result\")\n\n#7 matchedBy=fts+vector discovery/catalog.py:26-27\nsource:\n26\tdef get_workflow(self, workflow_id: str) -> Workflow | None:\n27\t        return self.workflows.get(workflow_id)\n\n#8 matchedBy=fts+vector discovery/state.py:15-16\nsource:\n15\tdef contains(self, workflow_id: str) -> bool:\n16\t        return workflow_id in self.ids\n\n#9 matchedBy=fts+vector tools/workflows.py:31-32\nsource:\n31\tdef is_workflow_in_discovery_result(workflow_id: str, result: DiscoveryResult) -> bool:\n32\t    return workflow_id == result.recommended or workflow_id in (result.alternatives or [])\n\n#10 matchedBy=fts+vector selection/validator.py:15-16\nsource:\n15\tdef set_workflow_state(self, state: WorkflowState) -> None:\n16\t        self.workflow_state = state\n",
        "stderr": ""
      }
    },
    {
      "id": "interactive-selection-guard",
      "query": "Where does interactive selection reject a workflow absent from the current discovery result?",
      "response": {
        "stdout": "query groups (1):\nQ1 [primary]: Where does interactive selection reject a workflow absent from the current discovery result?\nhits: 10\n\n#1 matchedBy=fts+vector tools/workflows.py:35-37\nsource:\n35\tdef authorize_selection_driver(workflow_id: str, result: DiscoveryResult) -> None:\n36\t    if not is_workflow_in_discovery_result(workflow_id, result):\n37\t        raise ValueError(\"workflow was not returned by discovery\")\n\n#2 matchedBy=fts+vector tools/workflows.py:31-32\nsource:\n31\tdef is_workflow_in_discovery_result(workflow_id: str, result: DiscoveryResult) -> bool:\n32\t    return workflow_id == result.recommended or workflow_id in (result.alternatives or [])\n\n#3 matchedBy=fts+vector tools/workflows.py:40-47\nsource:\n40\tdef handle_select_workflow(\n41\t    workflow_id: str, result: DiscoveryResult, catalog: WorkflowCatalog\n42\t) -> WorkflowResult:\n43\t    authorize_selection_driver(workflow_id, result)\n44\t    workflow = catalog.get_workflow(workflow_id)\n45\t    if workflow is None:\n46\t        raise KeyError(workflow_id)\n47\t    return apply_selected_workflow(workflow, result)\n\n#4 matchedBy=fts+vector tools/workflows.py:54-56\nsource:\n54\tdef lookup_discovered_parameters(workflow_id: str, result: DiscoveryResult) -> dict[str, str]:\n55\t    source = \"recommended\" if workflow_id == result.recommended else \"alternative\"\n56\t    return {\"source\": source}\n\n#5 matchedBy=fts+vector selection/validator.py:23-25\nsource:\n23\tdef validate(self, workflow_id: str) -> None:\n24\t        if not self.is_allowed(workflow_id):\n25\t            raise ValidationError(\"workflow was not in the current discovery result\")\n\n#6 matchedBy=fts+vector selection/validator.py:10-25\nsource:\n10\tclass Validator:\n11\t    def __init__(self, allowed_ids: set[str]) -> None:\n12\t        self.allowed_ids = allowed_ids\n13\t        self.workflow_state: WorkflowState | None = None\n14\t\n15\t    def set_workflow_state(self, state: WorkflowState) -> None:\n16\t        self.workflow_state = state\n17\t\n18\t    def is_allowed(self, workflow_id: str) -> bool:\n19\t        if workflow_id not in self.allowed_ids:\n20\t            return False\n21\t        return self.workflow_state is None or self.workflow_state.contains(workflow_id)\n22\t\n23\t    def validate(self, workflow_id: str) -> None:\n24\t        if not self.is_allowed(workflow_id):\n25\t            raise ValidationError(\"workflow was not in the current discovery result\")\n\n#7 matchedBy=fts+vector discovery/state.py:19-21\nsource:\n19\tdef state_from_context(context: dict[str, object]) -> WorkflowState:\n20\t    state = context.get(\"workflow_state\")\n21\t    return state if isinstance(state, WorkflowState) else WorkflowState()\n\n#8 matchedBy=fts+vector tools/workflows.py:50-51\nsource:\n50\tdef apply_selected_workflow(workflow: Workflow, result: DiscoveryResult) -> WorkflowResult:\n51\t    return WorkflowResult(workflow.workflow_id, lookup_discovered_parameters(workflow.workflow_id, result))\n\n#9 matchedBy=fts+vector selection/retry.py:14-16\nsource:\n14\tdef self_correct_selection(validator: Validator, workflow_id: str) -> str:\n15\t    validator.validate(workflow_id)\n16\t    return workflow_id\n\n#10 matchedBy=fts+vector discovery/catalog.py:6-10\nsource:\n6\t@dataclass\n7\tclass Workflow:\n8\t    workflow_id: str\n9\t    parameters: dict[str, str] = field(default_factory=dict)\n10\t    labels: dict[str, str] = field(default_factory=dict)\n",
        "stderr": ""
      }
    },
    {
      "id": "validate-workflow-parameters",
      "query": "Where are remediation workflow parameters validated before execution?",
      "response": {
        "stdout": "query groups (1):\nQ1 [primary]: Where are remediation workflow parameters validated before execution?\nhits: 10\n\n#1 matchedBy=fts+vector tools/workflows.py:50-51\nsource:\n50\tdef apply_selected_workflow(workflow: Workflow, result: DiscoveryResult) -> WorkflowResult:\n51\t    return WorkflowResult(workflow.workflow_id, lookup_discovered_parameters(workflow.workflow_id, result))\n\n#2 matchedBy=fts+vector tools/workflows.py:40-47\nsource:\n40\tdef handle_select_workflow(\n41\t    workflow_id: str, result: DiscoveryResult, catalog: WorkflowCatalog\n42\t) -> WorkflowResult:\n43\t    authorize_selection_driver(workflow_id, result)\n44\t    workflow = catalog.get_workflow(workflow_id)\n45\t    if workflow is None:\n46\t        raise KeyError(workflow_id)\n47\t    return apply_selected_workflow(workflow, result)\n\n#3 matchedBy=fts+vector discovery/catalog.py:14-15\nsource:\n14\tdef __init__(self, workflows: list[Workflow]) -> None:\n15\t        self.workflows = {workflow.workflow_id: workflow for workflow in workflows}\n\n#4 matchedBy=fts+vector discovery/catalog.py:13-27\nsource:\n13\tclass WorkflowCatalog:\n14\t    def __init__(self, workflows: list[Workflow]) -> None:\n15\t        self.workflows = {workflow.workflow_id: workflow for workflow in workflows}\n16\t\n17\t    def list_workflows(self, filters: dict[str, str] | None = None) -> list[Workflow]:\n18\t        workflows = list(self.workflows.values())\n19\t        if not filters:\n20\t            return workflows\n21\t        return [workflow for workflow in workflows if all(workflow.labels.get(k) == v for k, v in filters.items())]\n22\t\n23\t    def list_actions(self, filters: dict[str, str]) -> list[Workflow]:\n24\t        return self.list_workflows(filters)\n25\t\n26\t    def get_workflow(self, workflow_id: str) -> Workflow | None:\n27\t        return self.workflows.get(workflow_id)\n\n#5 matchedBy=fts+vector tools/workflows.py:15-18\nsource:\n15\t@dataclass\n16\tclass WorkflowResult:\n17\t    workflow_id: str\n18\t    parameters: dict[str, str]\n\n#6 matchedBy=fts+vector execution/runner.py:4-11\nsource:\n4\tdef validate_workflow_parameters(\n5\t    required: set[str], supplied: dict[str, str]\n6\t) -> dict[str, str]:\n7\t    missing = required - supplied.keys()\n8\t    unknown = supplied.keys() - required\n9\t    if missing or unknown:\n10\t        raise ValueError(f\"invalid workflow parameters: missing={missing}, unknown={unknown}\")\n11\t    return supplied\n\n#7 matchedBy=fts+vector discovery/catalog.py:6-10\nsource:\n6\t@dataclass\n7\tclass Workflow:\n8\t    workflow_id: str\n9\t    parameters: dict[str, str] = field(default_factory=dict)\n10\t    labels: dict[str, str] = field(default_factory=dict)\n\n#8 matchedBy=fts+vector tools/workflows.py:54-56\nsource:\n54\tdef lookup_discovered_parameters(workflow_id: str, result: DiscoveryResult) -> dict[str, str]:\n55\t    source = \"recommended\" if workflow_id == result.recommended else \"alternative\"\n56\t    return {\"source\": source}\n\n#9 matchedBy=fts+vector selection/validator.py:23-25\nsource:\n23\tdef validate(self, workflow_id: str) -> None:\n24\t        if not self.is_allowed(workflow_id):\n25\t            raise ValidationError(\"workflow was not in the current discovery result\")\n\n#10 matchedBy=fts+vector discovery/state.py:12-13\nsource:\n12\tdef add(self, workflow_ids: list[str]) -> None:\n13\t        self.ids.update(workflow_id for workflow_id in workflow_ids if workflow_id)\n",
        "stderr": ""
      }
    },
    {
      "id": "empty-discovery-fails-closed",
      "query": "What happens when workflow discovery returns no IDs and a selection is attempted?",
      "response": {
        "stdout": "query groups (1):\nQ1 [primary]: What happens when workflow discovery returns no IDs and a selection is attempted?\nhits: 10\n\n#1 matchedBy=fts+vector tools/workflows.py:35-37\nsource:\n35\tdef authorize_selection_driver(workflow_id: str, result: DiscoveryResult) -> None:\n36\t    if not is_workflow_in_discovery_result(workflow_id, result):\n37\t        raise ValueError(\"workflow was not returned by discovery\")\n\n#2 matchedBy=fts+vector tools/workflows.py:31-32\nsource:\n31\tdef is_workflow_in_discovery_result(workflow_id: str, result: DiscoveryResult) -> bool:\n32\t    return workflow_id == result.recommended or workflow_id in (result.alternatives or [])\n\n#3 matchedBy=fts+vector discovery/state.py:6-16\nsource:\n6\t@dataclass\n7\tclass WorkflowState:\n8\t    \"\"\"Hold workflow IDs returned by discovery for later validation.\"\"\"\n9\t\n10\t    ids: set[str] = field(default_factory=set)\n11\t\n12\t    def add(self, workflow_ids: list[str]) -> None:\n13\t        self.ids.update(workflow_id for workflow_id in workflow_ids if workflow_id)\n14\t\n15\t    def contains(self, workflow_id: str) -> bool:\n16\t        return workflow_id in self.ids\n\n#4 matchedBy=fts+vector tools/workflows.py:40-47\nsource:\n40\tdef handle_select_workflow(\n41\t    workflow_id: str, result: DiscoveryResult, catalog: WorkflowCatalog\n42\t) -> WorkflowResult:\n43\t    authorize_selection_driver(workflow_id, result)\n44\t    workflow = catalog.get_workflow(workflow_id)\n45\t    if workflow is None:\n46\t        raise KeyError(workflow_id)\n47\t    return apply_selected_workflow(workflow, result)\n\n#5 matchedBy=fts+vector discovery/state.py:12-13\nsource:\n12\tdef add(self, workflow_ids: list[str]) -> None:\n13\t        self.ids.update(workflow_id for workflow_id in workflow_ids if workflow_id)\n\n#6 matchedBy=fts+vector selection/validator.py:10-25\nsource:\n10\tclass Validator:\n11\t    def __init__(self, allowed_ids: set[str]) -> None:\n12\t        self.allowed_ids = allowed_ids\n13\t        self.workflow_state: WorkflowState | None = None\n14\t\n15\t    def set_workflow_state(self, state: WorkflowState) -> None:\n16\t        self.workflow_state = state\n17\t\n18\t    def is_allowed(self, workflow_id: str) -> bool:\n19\t        if workflow_id not in self.allowed_ids:\n20\t            return False\n21\t        return self.workflow_state is None or self.workflow_state.contains(workflow_id)\n22\t\n23\t    def validate(self, workflow_id: str) -> None:\n24\t        if not self.is_allowed(workflow_id):\n25\t            raise ValidationError(\"workflow was not in the current discovery result\")\n\n#7 matchedBy=fts+vector selection/validator.py:18-21\nsource:\n18\tdef is_allowed(self, workflow_id: str) -> bool:\n19\t        if workflow_id not in self.allowed_ids:\n20\t            return False\n21\t        return self.workflow_state is None or self.workflow_state.contains(workflow_id)\n\n#8 matchedBy=fts+vector selection/validator.py:23-25\nsource:\n23\tdef validate(self, workflow_id: str) -> None:\n24\t        if not self.is_allowed(workflow_id):\n25\t            raise ValidationError(\"workflow was not in the current discovery result\")\n\n#9 matchedBy=fts+vector metrics/validator.py:4-5\nsource:\n4\tdef validate_metric_name(name: str) -> bool:\n5\t    return bool(name) and name.replace(\"_\", \"\").isalnum()\n\n#10 matchedBy=fts+vector discovery/catalog.py:6-10\nsource:\n6\t@dataclass\n7\tclass Workflow:\n8\t    workflow_id: str\n9\t    parameters: dict[str, str] = field(default_factory=dict)\n10\t    labels: dict[str, str] = field(default_factory=dict)\n",
        "stderr": ""
      }
    },
    {
      "id": "copy-selected-workflow-details",
      "query": "How are selected workflow details copied into the final investigation result?",
      "response": {
        "stdout": "query groups (1):\nQ1 [primary]: How are selected workflow details copied into the final investigation result?\nhits: 10\n\n#1 matchedBy=fts+vector tools/workflows.py:50-51\nsource:\n50\tdef apply_selected_workflow(workflow: Workflow, result: DiscoveryResult) -> WorkflowResult:\n51\t    return WorkflowResult(workflow.workflow_id, lookup_discovered_parameters(workflow.workflow_id, result))\n\n#2 matchedBy=fts+vector tools/workflows.py:40-47\nsource:\n40\tdef handle_select_workflow(\n41\t    workflow_id: str, result: DiscoveryResult, catalog: WorkflowCatalog\n42\t) -> WorkflowResult:\n43\t    authorize_selection_driver(workflow_id, result)\n44\t    workflow = catalog.get_workflow(workflow_id)\n45\t    if workflow is None:\n46\t        raise KeyError(workflow_id)\n47\t    return apply_selected_workflow(workflow, result)\n\n#3 matchedBy=fts+vector tools/workflows.py:31-32\nsource:\n31\tdef is_workflow_in_discovery_result(workflow_id: str, result: DiscoveryResult) -> bool:\n32\t    return workflow_id == result.recommended or workflow_id in (result.alternatives or [])\n\n#4 matchedBy=fts+vector tools/workflows.py:35-37\nsource:\n35\tdef authorize_selection_driver(workflow_id: str, result: DiscoveryResult) -> None:\n36\t    if not is_workflow_in_discovery_result(workflow_id, result):\n37\t        raise ValueError(\"workflow was not returned by discovery\")\n\n#5 matchedBy=fts+vector tools/workflows.py:54-56\nsource:\n54\tdef lookup_discovered_parameters(workflow_id: str, result: DiscoveryResult) -> dict[str, str]:\n55\t    source = \"recommended\" if workflow_id == result.recommended else \"alternative\"\n56\t    return {\"source\": source}\n\n#6 matchedBy=fts+vector discovery/catalog.py:26-27\nsource:\n26\tdef get_workflow(self, workflow_id: str) -> Workflow | None:\n27\t        return self.workflows.get(workflow_id)\n\n#7 matchedBy=fts+vector selection/validator.py:23-25\nsource:\n23\tdef validate(self, workflow_id: str) -> None:\n24\t        if not self.is_allowed(workflow_id):\n25\t            raise ValidationError(\"workflow was not in the current discovery result\")\n\n#8 matchedBy=fts+vector selection/validator.py:10-25\nsource:\n10\tclass Validator:\n11\t    def __init__(self, allowed_ids: set[str]) -> None:\n12\t        self.allowed_ids = allowed_ids\n13\t        self.workflow_state: WorkflowState | None = None\n14\t\n15\t    def set_workflow_state(self, state: WorkflowState) -> None:\n16\t        self.workflow_state = state\n17\t\n18\t    def is_allowed(self, workflow_id: str) -> bool:\n19\t        if workflow_id not in self.allowed_ids:\n20\t            return False\n21\t        return self.workflow_state is None or self.workflow_state.contains(workflow_id)\n22\t\n23\t    def validate(self, workflow_id: str) -> None:\n24\t        if not self.is_allowed(workflow_id):\n25\t            raise ValidationError(\"workflow was not in the current discovery result\")\n\n#9 matchedBy=fts+vector discovery/catalog.py:14-15\nsource:\n14\tdef __init__(self, workflows: list[Workflow]) -> None:\n15\t        self.workflows = {workflow.workflow_id: workflow for workflow in workflows}\n\n#10 matchedBy=fts+vector discovery/catalog.py:6-10\nsource:\n6\t@dataclass\n7\tclass Workflow:\n8\t    workflow_id: str\n9\t    parameters: dict[str, str] = field(default_factory=dict)\n10\t    labels: dict[str, str] = field(default_factory=dict)\n",
        "stderr": ""
      }
    }
  ]
}
